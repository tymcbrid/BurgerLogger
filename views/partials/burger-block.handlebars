<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if devoured}}I'M FULL{{else}}TIME TO EAT{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>
